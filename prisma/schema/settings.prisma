model SettingsPage {
  id        String          @id @default(uuid())
  key       SettingsPageKey @unique
  title     String?
  content   String
  isActive  Boolean         @default(true)
  createdAt DateTime        @default(now())
  updatedAt DateTime        @updatedAt

  @@map("settings_pages")
}
