// Catalog time-bucket row in `sessions` (shared definitions). Linked from `Event.sessionId` only.
model Session {
  id           String              @id @default(uuid())
  session      SessionBucketType
  sessionValue String
  sessionLevel String              @unique
  year         String
  creationMode SessionCreationMode @default(Manual)
  isActive     Boolean             @default(true)
  events       Event[]             @relation("EventCatalogSession")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([year])
  @@map("sessions")
}

model Event {
  id                      String            @id @default(uuid())
  // ── Permanent Identity (Step 1) ───────────────────────────
  baseEventName           String?
  eventName               String
  eventYear               String?
  scheduleLabel           String?           @map("eventSessionLabel")
  coverImage              String
  programId               String
  program                 Program           @relation(fields: [programId], references: [id])
  organizer               String
  location                String?
  eventPortal             String
  registrationPortal      String
  description             String
  note                    String?
  creationMode            EventCreationMode @default(Manual)
  sourceEventId           String?
  sourceEvent             Event?            @relation("EventAutoSource", fields: [sourceEventId], references: [id], onDelete: SetNull)
  generatedEvents         Event[]           @relation("EventAutoSource")
  sessionId               String?
  catalogSession          Session?          @relation("EventCatalogSession", fields: [sessionId], references: [id], onDelete: Restrict)
  isSharedToCommunity     Boolean           @default(false)
  isUserAgreementAccepted Boolean           @default(false)
  // ── Repeat Configuration ──────────────────────────────────
  repeatConfig            RepeatConfig?
  schedule                EventSchedule?
  groups                  EventGroup[]

  results EventResult[]
  eventApplieds EventApplied[]

  editLogs     EditLog[]
  version      Int       @default(1) // optimistic locking
  creatorId    String
  creator      User      @relation("EventCreator", fields: [creatorId], references: [id], onDelete: Restrict)
  lastEditorId String?
  lastEditor   User?     @relation("EventLastEditor", fields: [lastEditorId], references: [id], onDelete: SetNull)
  isPublished  Boolean   @default(true)
  isActive     Boolean   @default(true)
  isVerified   Boolean   @default(false)
  isDisabled   Boolean   @default(false) // admin: hide event from feeds / public detail without soft-delete
  isDeleted    Boolean   @default(false)
  deletedAt    DateTime? // soft delete timestamp

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([programId])
  @@index([eventName])
  @@index([creatorId])
  @@index([lastEditorId])
  @@index([creationMode])
  @@index([sourceEventId])
  @@index([sessionId])
  @@index([isDisabled])
  @@map("events")
}

/// Per-event registration window and pricing only. Catalog `Session` lives on `Event.sessionId`.
model EventSchedule {
  id               String           @id @default(uuid())
  eventId          String           @unique
  event            Event            @relation(fields: [eventId], references: [id], onDelete: Cascade)
  competitionLevel CompetitionLevel
  eventType        EventType
  registrationDate DateTime
  deadline         DateTime
  cost             Decimal          @db.Decimal(10, 2)
  hasFinalDeadline Boolean          @default(false)
  finalDeadline    DateTime?
  lateFee          Decimal          @default(0) @db.Decimal(10, 2) // fixed amount; 0 = no late fee

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("event_schedules")
}

model EventGroup {
  id        String         @id @default(uuid())
  eventId   String
  event     Event          @relation(fields: [eventId], references: [id], onDelete: Cascade)
  name      String
  criteria  GroupCriteria
  condition RoundCondition
  value     Int
  rounds    EventRound[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([eventId])
  @@map("event_groups")
}

model EventRound {
  id               String           @id @default(uuid())
  groupId          String
  group            EventGroup       @relation(fields: [groupId], references: [id], onDelete: Cascade)
  roundType        CompetitionLevel
  deadline         DateTime
  cost             Decimal          @db.Decimal(10, 2)
  hasFinalDeadline Boolean          @default(false)
  finalDeadline    DateTime?
  lateFee          Decimal          @default(0) @db.Decimal(10, 2) // fixed amount; 0 = no late fee
  description      String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([groupId])
  @@map("event_rounds")
}

model RepeatConfig {
  id                   String          @id @default(uuid())
  eventId              String          @unique
  event                Event           @relation(fields: [eventId], references: [id], onDelete: Cascade)
  frequency            RepeatFrequency @default(DontRepeat)
  startDate            DateTime?
  nextAutoGenerateDate DateTime?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("repeat_configs")
}

model EventResult {
  id        String        @id @default(uuid())
  eventId   String
  event     Event         @relation(fields: [eventId], references: [id], onDelete: Cascade)
  placement PlacementType
  note      String?
  createdAt DateTime      @default(now())

  @@unique([eventId, placement])
  @@map("event_results")
}

model EditLog {
  id      String @id @default(uuid())
  eventId String
  event   Event  @relation(fields: [eventId], references: [id], onDelete: Cascade)

  version        Int // which Event version this edit created
  editorId       String
  editor         User     @relation(fields: [editorId], references: [id], onDelete: Restrict)
  changedFields  String[]
  previousValues Json

  createdAt DateTime @default(now())

  @@index([eventId])
  @@index([createdAt])
  @@index([editorId])
  @@map("edit_logs")
}
